<form script="table_icons_rangemap.js" stylesheet="table_decorations.css">
  <label>Overview</label>
  <row>
    <panel>
      <title>Environment check result</title>
      <table id="overview_status_table">
        <search>
          <query>index="splunk_checker" source="warning_msg" | stats first(info{}) as "Warning" by sourcetype,cluster_id | fields cluster_id, Warning | eval Warning = if(isnull(Warning), "No", "Yes") | dedup cluster_id sortby _time | eval Status = if(Warning == "No", "low", "severe") | fields cluster_id, Status</query>
          <earliest>-15m</earliest>
          <latest>now</latest>
        </search>
        <option name="wrap">true</option>
        <option name="rowNumbers">false</option>
        <option name="drilldown">none</option>
        <option name="dataOverlayMode">none</option>
        <option name="count">10</option>
      </table>
    </panel>
  </row>
  <row>
    <panel>
      <title>Cluster check result</title>
      <input type="dropdown" token="cluster_id" searchWhenChanged="true">
        <label>Cluster Name</label>
        <search>
          <query>index="splunk_checker" | stats count by cluster_id</query>
          <earliest>-12h@h</earliest>
          <latest>now</latest>
        </search>
        <fieldForLabel>cluster_id</fieldForLabel>
        <fieldForValue>cluster_id</fieldForValue>
      </input>
      <table id="cluster_status_table">
        <search>
          <query>index="splunk_checker" source="warning_msg" cluster_id=$cluster_id$  | rename info{} as "Warnings", sourcetype as "Item"| table Item, Warnings | dedup Item sortby -_time | eval Status=case(isnull(Warnings), "low", like(Warnings,"%[The check is skipped due to http exception.]"), "unknown", 1==1, "severe")</query>
          <earliest>-15m</earliest>
          <latest>now</latest>
        </search>
        <option name="drilldown">row</option>
        <option name="wrap">true</option>
        <option name="rowNumbers">false</option>
        <option name="dataOverlayMode">none</option>
        <option name="count">10</option>
      </table>
    </panel>
  </row>
</form>